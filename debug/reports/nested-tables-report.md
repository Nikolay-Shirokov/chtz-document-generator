# Анализ вложенных таблиц в документе

**Файл:** `tests/test1-original/Очередь1_Реализация_изменений_функционала_МЧД_минимум_1.docx`

**Дата анализа:** 23 января 2026

---

## Общая статистика

- **Всего таблиц в документе:** 9
- **Основных таблиц (верхнего уровня):** 7
- **Вложенных таблиц:** 2
- **Таблиц, содержащих вложенные:** 2

---

## Найденные вложенные таблицы

### 🔹 Вложенная таблица #1

**Расположение:**
- Находится внутри **Основной таблицы #2** (Таблица 2 в документе)
- Название родительской таблицы: **"Условия определения общего результата проверки МЧД контрагента"**

**Контекст родительской таблицы:**
```
Функция: Фиксация условий определения общего результата проверки МЧД контрагента
(при получении подписи контрагента по входящим и исходящим ЭД)
```

**Расположение в родительской таблице:**
- Находится в ячейке "Сценарий" (строка 3)
- Текст перед вложенной таблицей: "1. Условия определения общего результата проверки МЧД контрагента (при получении)."

**Структура вложенной таблицы:**
- **Количество строк:** 4
- **Количество столбцов:** 2
- **Тип:** Справочная таблица с условиями

**Содержимое:**

| Общий результат проверки МЧД | Условия для определения результата |
|------------------------------|-----------------------------------|
| Обнаружены ошибки | Условия, при которых общий результат проверки будет считаться неудачным (И/ИЛИ):<br>• При условии, что в справочнике «Правила проверки МЧД (ЭДО) для элемента справочника «(При получении) Проверка полномочий» установлено значение «Реакция» = «Блокировать»<br>• нет полномочий из МЧД<br>• нет сопоставления полномочий<br>• отсутствует указание Вида ЭД<br>• Не соблюдается хотя бы одно из правил проверки МЧД |
| Проверка пройдена частично | При выполнении проверок, выполняются условия, определяющие успешный результат, за исключением (И/ИЛИ):<br>• Не соблюдается хотя бы одно из правил проверки МЧД (при получении), для которых установлена реакция «Предупреждать»<br>• В элементе справочника «Доверенности (ЭДО)» для данной МЧД есть хотя бы одно полномочие, где «Вид проверки» = «Ручная» |
| Проверка пройдена успешно | Условия, при которых общий результат проверки будет считаться успешным (И):<br>• В справочнике «Правила проверки МЧД (ЭДО) установлено значение «Реакция» = «Блокировать»<br>• Хотя бы по одной из строк есть указание Вида ЭД<br>• Соблюдаются все правила проверки МЧД |

**Назначение:**
Таблица определяет условия для трех возможных результатов проверки МЧД (машиночитаемой доверенности) контрагента при получении входящих/исходящих электронных документов.

---

### 🔹 Вложенная таблица #2

**Расположение:**
- Находится внутри **Основной таблицы #7** (Таблица 8 в документе)
- Название родительской таблицы: **"Условия определения общего результата проверки МЧД подписанта в СТИУМ"**

**Контекст родительской таблицы:**
```
Функция: Фиксация условий определения общего результата проверки МЧД подписанта в СТИУМ
(при подписании входящего и исходящего ЭД)
```

**Расположение в родительской таблице:**
- Находится в ячейке "Сценарий" (строка 3)
- Текст перед вложенной таблицей: "1. Условия определения общего результата проверки МЧД подписанта в СТИУМ (при подписании)."

**Структура вложенной таблицы:**
- **Количество строк:** 4
- **Количество столбцов:** 2
- **Тип:** Справочная таблица с условиями

**Содержимое:**

| Общий результат проверки МЧД | Условия для определения результата |
|------------------------------|-----------------------------------|
| Обнаружены ошибки | Условия, при которых общий результат проверки будет считаться неудачным (И/ИЛИ):<br>• При условии, что в справочнике «Правила проверки МЧД (ЭДО) для элемента справочника «(При подписании) Проверка полномочий» установлено значение «Реакция» = «Блокировать»<br>• нет полномочий из МЧД<br>• нет сопоставления полномочий<br>• отсутствует указание Вида ЭД<br>• Не соблюдается хотя бы одно из правил проверки МЧД |
| Проверка пройдена частично | При выполнении проверок, выполняются условия, определяющие успешный результат, за исключением (И/ИЛИ):<br>• Не соблюдается хотя бы одно из правил проверки МЧД (при подписании), для которых установлена реакция «Предупреждать»<br>• В элементе справочника «Доверенности (ЭДО)» для данной МЧД есть хотя бы одно полномочие, где «Вид проверки» = «Ручная» |
| Проверка пройдена успешно | Условия, при которых общий результат проверки будет считаться успешным (И):<br>• В справочнике «Правила проверки МЧД (ЭДО) установлено значение «Реакция» = «Блокировать»<br>• Хотя бы по одной из строк есть указание Вида ЭД<br>• Соблюдаются все правила проверки МЧД |

**Назначение:**
Таблица определяет условия для трех возможных результатов проверки МЧД подписанта в СТИУМ при подписании входящих/исходящих электронных документов.

---

## Структура документа

```
📄 Документ: Очередь1_Реализация_изменений_функционала_МЧД_минимум_1.docx
│
├─ 📊 Таблица 1: Интерфейс для входящих ЭД в АРМ «Модуля ЭДО»
│
├─ 📊 Таблица 2: Условия определения общего результата проверки МЧД контрагента
│  │
│  └─ 📋 ВЛОЖЕННАЯ ТАБЛИЦА #1 (в ячейке "Сценарий")
│     └─ Условия определения результата (при получении)
│        ├─ Обнаружены ошибки
│        ├─ Проверка пройдена частично
│        └─ Проверка пройдена успешно
│
├─ 📊 Таблица 3: Интерфейс для исходящих ЭД в АРМ «Модуля ЭДО»
│
├─ 📊 Таблица 4: Работа с формой «Анализ расхождений»
│
├─ 📊 Таблица 6: Подписание входящих ЭД
│
├─ 📊 Таблица 7: Подписание исходящих ЭД
│
└─ 📊 Таблица 8: Условия определения общего результата проверки МЧД подписанта в СТИУМ
   │
   └─ 📋 ВЛОЖЕННАЯ ТАБЛИЦА #2 (в ячейке "Сценарий")
      └─ Условия определения результата (при подписании)
         ├─ Обнаружены ошибки
         ├─ Проверка пройдена частично
         └─ Проверка пройдена успешно
```

---

## Особенности вложенных таблиц

### Сходства между двумя вложенными таблицами:

1. **Идентичная структура:** Обе таблицы имеют 4 строки и 2 столбца
2. **Одинаковые заголовки:** "Общий результат проверки МЧД" и "Условия для определения результата"
3. **Три типа результатов:**
   - Обнаружены ошибки
   - Проверка пройдена частично
   - Проверка пройдена успешно
4. **Расположение:** Обе находятся в ячейке "Сценарий" соответствующих функциональных таблиц
5. **Контекст:** Обе связаны с проверкой МЧД (машиночитаемых доверенностей)

### Различия:

1. **Вложенная таблица #1:**
   - Относится к проверке МЧД **контрагента**
   - Применяется **при получении** ЭД
   - Условия для справочника "(При получении) Проверка полномочий"

2. **Вложенная таблица #2:**
   - Относится к проверке МЧД **подписанта в СТИУМ**
   - Применяется **при подписании** ЭД
   - Условия для справочника "(При подписании) Проверка полномочий"

---

## XML структура

В XML структуре документа (word/document.xml) вложенные таблицы представлены следующим образом:

```xml
<w:tbl>  <!-- Основная таблица -->
  <w:tr>  <!-- Строка таблицы -->
    <w:tc>  <!-- Ячейка таблицы -->
      <w:p>  <!-- Параграф с текстом -->
        <w:t>1. Условия определения...</w:t>
      </w:p>
      <w:tbl>  <!-- ВЛОЖЕННАЯ ТАБЛИЦА внутри ячейки -->
        <w:tr>
          <w:tc>...</w:tc>
        </w:tr>
      </w:tbl>
    </w:tc>
  </w:tr>
</w:tbl>
```

Ключевой момент: элемент `<w:tbl>` (таблица) находится непосредственно внутри элемента `<w:tc>` (ячейка таблицы), что делает его вложенной таблицей.

---

## Проблема с текущей конвертацией

**КРИТИЧНО:** Вложенные таблицы **не сконвертированы** в текущем MD файле!

При анализе файла `tests/test1-original/Очередь1_Реализация_изменений_функционала_МЧД_минимум_1.md` обнаружено:

**Таблица 2 (строки 46-52):**
```markdown
#### Таблица 2. Условия определения общего результата проверки МЧД контрагента

:::function-table{#func-фиксация-условий-определения}
function: Фиксация условий определения общего результата проверки МЧД контрагента...
scenario: |
  1. Условия определения общего результата проверки МЧД контрагента (при получении).
:::
```

**Таблица 8 (строки 145-151):**
```markdown
#### Таблица 8. Условия определения общего результата проверки МЧД подписанта в СТИУМ

:::function-table{#func-фиксация-условий-определения}
function: Фиксация условий определения общего результата проверки МЧД подписанта в СТИУМ...
scenario: |
  1. Условия определения общего результата проверки МЧД подписанта в СТИУМ (при подписании).
:::
```

В обоих случаях текст "scenario" обрывается на вводной фразе, а сама **вложенная таблица с условиями полностью отсутствует**.

Это означает **потерю критически важной информации** при конвертации DOCX → MD.

---

## Выводы

1. В документе присутствуют **2 вложенные таблицы**, обе имеют одинаковую структуру и назначение
2. Обе вложенные таблицы содержат **критически важную информацию** о логике определения результатов проверки МЧД
3. Вложенные таблицы расположены в **функциональных таблицах**, описывающих сценарии работы с ЭДО
4. Такая структура используется для **компактного представления сложных условий** внутри описания сценариев
5. **ТЕКУЩИЙ КОНВЕРТЕР НЕ ОБРАБАТЫВАЕТ ВЛОЖЕННЫЕ ТАБЛИЦЫ** - они полностью теряются при конвертации
6. При создании обратного конвертера (Markdown → DOCX) необходимо **учитывать вложенные таблицы** и предусмотреть их поддержку

---

## Рекомендации для конвертера

### Для обратного конвертера (DOCX → Markdown):

1. **Обнаруживать вложенные таблицы** путем анализа XML структуры (`w:tbl` внутри `w:tc`)
2. **Использовать отступы или специальную разметку** для отображения вложенности в Markdown
3. **Сохранять контекст** (текст перед вложенной таблицей в родительской ячейке)
4. **Учитывать, что Markdown не поддерживает вложенные таблицы нативно**, поэтому потребуется специальное решение

### Варианты представления вложенных таблиц в Markdown:

#### Вариант 1: Специальная директива
```markdown
:::function-table
scenario: |
  1. Условия определения общего результата проверки МЧД контрагента (при получении).

  :::nested-table
  | Общий результат проверки МЧД | Условия для определения результата |
  |------------------------------|-----------------------------------|
  | Обнаружены ошибки | ... |
  | Проверка пройдена частично | ... |
  | Проверка пройдена успешно | ... |
  :::
:::
```

#### Вариант 2: Отступы (как код)
```markdown
:::function-table
scenario: |
  1. Условия определения общего результата проверки МЧД контрагента (при получении).

      | Общий результат проверки МЧД | Условия для определения результата |
      |------------------------------|-----------------------------------|
      | Обнаружены ошибки | ... |
```

#### Вариант 3: HTML (наиболее точный)
```markdown
:::function-table
scenario: |
  1. Условия определения общего результата проверки МЧД контрагента (при получении).

  <table>
    <tr>
      <th>Общий результат проверки МЧД</th>
      <th>Условия для определения результата</th>
    </tr>
    <tr>
      <td>Обнаружены ошибки</td>
      <td>...</td>
    </tr>
  </table>
:::
```

#### Вариант 4: Линейный список (упрощенный, без таблицы)
```markdown
:::function-table
scenario: |
  1. Условия определения общего результата проверки МЧД контрагента (при получении).

  **Общий результат проверки МЧД:**

  - **Обнаружены ошибки:** Условия, при которых...
  - **Проверка пройдена частично:** При выполнении проверок...
  - **Проверка пройдена успешно:** Условия, при которых...
:::
```

### Для прямого конвертера (Markdown → DOCX):

При обратной генерации DOCX из Markdown необходимо:

1. **Распознавать вложенные таблицы** в Markdown (по выбранной разметке)
2. **Создавать XML структуру с `w:tbl` внутри `w:tc`**
3. **Сохранять форматирование** (границы, стили) вложенных таблиц
4. **Правильно размещать текст до и после** вложенной таблицы в родительской ячейке

---

**Скрипты для анализа:**
- `find-nested-tables.js` - общий обзор вложенных таблиц
- `analyze-nested-tables-detailed.js` - детальный анализ с полным содержимым
